#include <stdio.h>

/**
 * main - Startingpoint
 * program that prints the lowercase alphabet in reverse
 * Return: Always 0 (Successful)
 */

int main(void)
{
	char t = 'z';

	while (t >= 'a')
	{
		putchar(c);
		t--;
	}
	putchar('\n');
	return (0);
}

