#include <stdio.h>

/**
 * main - Starting point
 * single digit numbers of base 10 starting from 0
 * Return: Always 0 (Successful)
 */

int main(void)
{
	int f = 48;

	while (f <58)
	{
		putchar(f);
		d++;
	}
	putchar('\n');
	return (0);
}
