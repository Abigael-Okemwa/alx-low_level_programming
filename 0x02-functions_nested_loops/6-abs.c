#include "main.h"
#include <stdio.h>
/**
 * _abs - for the absolute integer values
 * @n: the inegers to use
 * Return: int
 */

int _abs(int)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (0);
	}
}
